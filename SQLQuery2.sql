
                        SELECT Id, Name, Description, EventURL, 
                        StartDate, EndDate, EventImageURL, 
                        UserGroupId, LocationId, InActive
                        From Events
                         WHERE UserId = 12